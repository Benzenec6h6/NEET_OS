[package]
name = "system-init"
version = "0.1.0"
edition = "2021"

[dependencies]
init-core = { path = "../init-core" }
nix = { version = "0.31.3", features = ["fs", "user", "mount"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
